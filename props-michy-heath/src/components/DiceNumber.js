
const DiceNumber = (props) => {
    return (
        <>
        <h2>This is a Dice!</h2>
        <h1>{props.diceNum(props.diceNum[props.index])}</h1>

        </>
    )
    }

    export default DiceNumber
