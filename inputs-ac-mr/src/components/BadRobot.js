import React from 'react'
const BadRobot = (props) => {
  return whyAreYouBeeping.map( (input) => {
     return "B" + "L" + "A"
     
    // slicing rest of word makes the rest of the words not capital, pretty much cutting off the capitlization method from the rest
  })
  return (
    <>
      <h3>{BadRobot(props.userName)}</h3>
    </>
  )
}
export default BadRobot